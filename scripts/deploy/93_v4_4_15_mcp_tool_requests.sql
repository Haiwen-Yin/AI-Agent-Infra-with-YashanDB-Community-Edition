-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_mcp_requests.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_MCP_TOOL_REQUESTS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_MCP_TOOL_REQUESTS (REQUEST_ID VARCHAR(128) PRIMARY KEY, JOB_ID VARCHAR(128) NOT NULL UNIQUE REFERENCES EXECUTION_JOBS(JOB_ID), TOOL_ID VARCHAR(128) NOT NULL, PRINCIPAL_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), SECURITY_DOMAIN_ID VARCHAR(128) NOT NULL REFERENCES CX_SECURITY_DOMAINS(SECURITY_DOMAIN_ID), INSTANCE_ID VARCHAR(128) NOT NULL REFERENCES CX_AGENT_INSTANCES(INSTANCE_ID), TOKEN_DIGEST VARCHAR(64) NOT NULL REFERENCES CX_AGENT_ACCESS_TOKENS(TOKEN_DIGEST), FENCING_TOKEN INTEGER NOT NULL CHECK(FENCING_TOKEN>0), CONTRACT_DIGEST VARCHAR(64) NOT NULL, PAYLOAD_DIGEST VARCHAR(64) NOT NULL, REQUEST_DIGEST VARCHAR(64) NOT NULL, IDEMPOTENCY_KEY VARCHAR(128) NOT NULL, CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, UNIQUE(PRINCIPAL_ID,IDEMPOTENCY_KEY))'; END IF; END;
/

CREATE OR REPLACE TRIGGER CX86_36C6575FDD050E00 BEFORE DELETE OR UPDATE ON CX_MCP_TOOL_REQUESTS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_MCP_TOOL_REQUESTS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_MCP_TOOL_REQUESTS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_MCP_TOOL_REQUESTS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_MCP_TOOL_REQUESTS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/
