-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_native_context_sources.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_NATIVE_CONTEXT_REVISIONS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_NATIVE_CONTEXT_REVISIONS (REVISION_ID VARCHAR(128) PRIMARY KEY, FAMILY VARCHAR(16) NOT NULL CHECK(FAMILY IN (''TASK'',''GRAPH'',''DB4A2A'',''AUDIT'')), ENTITY_ID VARCHAR(128) NOT NULL, SECURITY_DOMAIN_ID VARCHAR(128) NOT NULL REFERENCES CX_SECURITY_DOMAINS(SECURITY_DOMAIN_ID), SOURCE_PRINCIPAL_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), CAPTURED_BY VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), CONTENT_DIGEST VARCHAR(64) NOT NULL, REASON VARCHAR(1000) NOT NULL, IDEMPOTENCY_KEY VARCHAR(128) NOT NULL, REQUEST_DIGEST VARCHAR(64) NOT NULL, CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, UNIQUE(CAPTURED_BY,IDEMPOTENCY_KEY), UNIQUE(REVISION_ID,FAMILY))'; END IF; END;
/

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_TASK_CONTEXT_REVISIONS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_TASK_CONTEXT_REVISIONS (REVISION_ID VARCHAR(128) PRIMARY KEY, FAMILY VARCHAR(16) DEFAULT ''TASK'' NOT NULL CHECK(FAMILY=''TASK''), GOAL CLOB, STATUS VARCHAR(32), PRIORITY INTEGER, STRATEGY VARCHAR(128), RESULT_SUMMARY CLOB, FOREIGN KEY(REVISION_ID,FAMILY) REFERENCES CX_NATIVE_CONTEXT_REVISIONS(REVISION_ID,FAMILY))'; END IF; END;
/

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_GRAPH_CONTEXT_REVISIONS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_GRAPH_CONTEXT_REVISIONS (REVISION_ID VARCHAR(128) PRIMARY KEY, FAMILY VARCHAR(16) DEFAULT ''GRAPH'' NOT NULL CHECK(FAMILY=''GRAPH''), GRAPH_VERSION_ID VARCHAR(128), PLAN_ID VARCHAR(128), STATUS VARCHAR(32), CURRENT_CHECKPOINT_ID VARCHAR(128), ERROR_CODE VARCHAR(128), ERROR_MESSAGE VARCHAR(2000), FOREIGN KEY(REVISION_ID,FAMILY) REFERENCES CX_NATIVE_CONTEXT_REVISIONS(REVISION_ID,FAMILY))'; END IF; END;
/

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_DB4A2A_CONTEXT_REVISIONS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_DB4A2A_CONTEXT_REVISIONS (REVISION_ID VARCHAR(128) PRIMARY KEY, FAMILY VARCHAR(16) DEFAULT ''DB4A2A'' NOT NULL CHECK(FAMILY=''DB4A2A''), TASK_ID VARCHAR(256), RECEIVER_AGENT_ID VARCHAR(128), CONTEXT_REF VARCHAR(256), SNAPSHOT_DIGEST VARCHAR(256), EXPECTED_VERSION NUMBER(19), SCOPE_REF VARCHAR(256), SOURCE_BRANCH VARCHAR(256), BRANCH_POLICY VARCHAR(32), TRANSPORT VARCHAR(32), STATUS VARCHAR(32), CHILD_BRANCH_ID VARCHAR(128), FOREIGN KEY(REVISION_ID,FAMILY) REFERENCES CX_NATIVE_CONTEXT_REVISIONS(REVISION_ID,FAMILY))'; END IF; END;
/

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_AUDIT_CONTEXT_REVISIONS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_AUDIT_CONTEXT_REVISIONS (REVISION_ID VARCHAR(128) PRIMARY KEY, FAMILY VARCHAR(16) DEFAULT ''AUDIT'' NOT NULL CHECK(FAMILY=''AUDIT''), ACTOR_TYPE VARCHAR(16), ACTION_NAME VARCHAR(128), RESOURCE_TYPE VARCHAR(64), RESOURCE_ID VARCHAR(128), OUTCOME VARCHAR(32), REASON VARCHAR(2000), FOREIGN KEY(REVISION_ID,FAMILY) REFERENCES CX_NATIVE_CONTEXT_REVISIONS(REVISION_ID,FAMILY))'; END IF; END;
/

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_TASK_CONTEXT_ITEMS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_TASK_CONTEXT_ITEMS (REVISION_ID VARCHAR(128) NOT NULL REFERENCES CX_TASK_CONTEXT_REVISIONS(REVISION_ID), ITEM_ID VARCHAR(128) NOT NULL, STEP_ORDER INTEGER, DESCRIPTION CLOB, TOOL_NAME VARCHAR(128), STATUS VARCHAR(32), PRIMARY KEY(REVISION_ID,ITEM_ID))'; END IF; END;
/

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_GRAPH_CONTEXT_ITEMS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_GRAPH_CONTEXT_ITEMS (REVISION_ID VARCHAR(128) NOT NULL REFERENCES CX_GRAPH_CONTEXT_REVISIONS(REVISION_ID), ITEM_ID VARCHAR(128) NOT NULL, NODE_KEY VARCHAR(256), STATUS VARCHAR(32), BRANCH_KEY VARCHAR(256), JOIN_KEY VARCHAR(256), ITERATION_NO INTEGER, INPUT_CHECKPOINT_ID VARCHAR(128), OUTPUT_CHECKPOINT_ID VARCHAR(128), PRIMARY KEY(REVISION_ID,ITEM_ID))'; END IF; END;
/

CREATE OR REPLACE TRIGGER CX86_6B2CB61D9C07FED3 BEFORE DELETE OR UPDATE ON CX_NATIVE_CONTEXT_REVISIONS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_NATIVE_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_NATIVE_CONTEXT_REVISIONS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

CREATE OR REPLACE TRIGGER CX86_1A857158C0B8C91C BEFORE DELETE OR UPDATE ON CX_TASK_CONTEXT_REVISIONS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_TASK_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_TASK_CONTEXT_REVISIONS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

CREATE OR REPLACE TRIGGER CX86_C8930EC07698333A BEFORE DELETE OR UPDATE ON CX_GRAPH_CONTEXT_REVISIONS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_GRAPH_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_GRAPH_CONTEXT_REVISIONS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

CREATE OR REPLACE TRIGGER CX86_C4B2D7B9AFBFA8AD BEFORE DELETE OR UPDATE ON CX_DB4A2A_CONTEXT_REVISIONS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_DB4A2A_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_DB4A2A_CONTEXT_REVISIONS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

CREATE OR REPLACE TRIGGER CX86_1043B6C8F7E85317 BEFORE DELETE OR UPDATE ON CX_AUDIT_CONTEXT_REVISIONS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_AUDIT_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_AUDIT_CONTEXT_REVISIONS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

CREATE OR REPLACE TRIGGER CX86_187277234B302F28 BEFORE DELETE OR UPDATE ON CX_TASK_CONTEXT_ITEMS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_TASK_CONTEXT_ITEMS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_TASK_CONTEXT_ITEMS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

CREATE OR REPLACE TRIGGER CX86_0B06049E984A8E61 BEFORE DELETE OR UPDATE ON CX_GRAPH_CONTEXT_ITEMS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_GRAPH_CONTEXT_ITEMS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_GRAPH_CONTEXT_ITEMS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_NATIVE_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_NATIVE_CONTEXT_REVISIONS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_TASK_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_TASK_CONTEXT_REVISIONS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_GRAPH_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_GRAPH_CONTEXT_REVISIONS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_DB4A2A_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_DB4A2A_CONTEXT_REVISIONS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_AUDIT_CONTEXT_REVISIONS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_AUDIT_CONTEXT_REVISIONS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_TASK_CONTEXT_ITEMS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_TASK_CONTEXT_ITEMS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_GRAPH_CONTEXT_ITEMS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_GRAPH_CONTEXT_ITEMS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/
