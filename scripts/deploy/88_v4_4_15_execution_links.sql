-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_execution_links.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_WORK_EXECUTION_LINKS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_WORK_EXECUTION_LINKS (WORK_CONTRACT_ID VARCHAR(128) NOT NULL REFERENCES CX_WORK_CONTRACTS(WORK_CONTRACT_ID), LINK_KIND VARCHAR(16) NOT NULL CHECK(LINK_KIND IN (''WORKSPACE'',''TASK'',''GRAPH'')), ENTITY_ID VARCHAR(128) NOT NULL, SOURCE_PRINCIPAL_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), CREATED_BY VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, PRIMARY KEY(WORK_CONTRACT_ID,LINK_KIND))'; END IF; END;
/

CREATE OR REPLACE TRIGGER CX86_FAB22E09ECF6F00A BEFORE DELETE OR UPDATE ON CX_WORK_EXECUTION_LINKS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_WORK_EXECUTION_LINKS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_WORK_EXECUTION_LINKS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/
