-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_runtime_credentials.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_RUNTIME_CONTEXT_CREDENTIALS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_RUNTIME_CONTEXT_CREDENTIALS (EXECUTION_ID VARCHAR(128) PRIMARY KEY REFERENCES CX_RUNTIME_CONTEXT_BINDINGS(EXECUTION_ID), INSTANCE_ID VARCHAR(128) NOT NULL REFERENCES CX_AGENT_INSTANCES(INSTANCE_ID), TOKEN_DIGEST VARCHAR(64) NOT NULL REFERENCES CX_AGENT_ACCESS_TOKENS(TOKEN_DIGEST), FENCING_TOKEN INTEGER NOT NULL CHECK(FENCING_TOKEN>0), CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL)'; END IF; END;
/

CREATE OR REPLACE TRIGGER CX86_7DF6C58399D72FEA BEFORE DELETE OR UPDATE ON CX_RUNTIME_CONTEXT_CREDENTIALS DECLARE continuity_immutable EXCEPTION; BEGIN RAISE continuity_immutable; END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM ALL_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_RUNTIME_CONTEXT_CREDENTIALS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_RUNTIME_CONTEXT_CREDENTIALS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

BEGIN FOR g IN (SELECT ROLE,PRIVILEGE FROM ROLE_TAB_PRIVS WHERE OWNER=USER AND TABLE_NAME='CX_RUNTIME_CONTEXT_CREDENTIALS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_RUNTIME_CONTEXT_CREDENTIALS FROM "'||REPLACE(g.ROLE,'"','""')||'"'; END LOOP; END;
/
